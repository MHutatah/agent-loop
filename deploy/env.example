# Claude — from `claude setup-token` on your laptop (long-lived).
CLAUDE_CODE_OAUTH_TOKEN=

# Codex uses ~/.codex/auth.json — copy it from the laptop after `codex login`.
# No variable needed here.

# Optional
AGENTLOOP_LOG=INFO
# AGENTLOOP_DRY_RUN=1
